#Configuración Apache: Seguridad, Aumento de rendimiendo, SEO

<IfModule mod_headers.c>
  Header always set X-Content-Type-Options nosniff # No permite cambiar el tipo de contenido
  Header always set X-Frame-Options DENY # No permite que se muestre en un iframe
  Header always set X-XSS-Protection "1; mode=block" # Bloquea scripts maliciosos
  Header always set Referrer-Policy "strict-origin-when-cross-origin"
  Header always set Permissions-Policy "geolocation=(), microphone=(), camera=()"
  
  # Permite solo contenido seguro
  Header always set Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' https://cdn.jsdelivr.net; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://cdn.jsdelivr.net; font-src 'self' https://fonts.gstatic.com https://cdn.jsdelivr.net; img-src 'self' data:; connect-src 'self' https://mihuelva-hw2j.onrender.com; frame-ancestors 'none';"
  
  # HTTPS siempre
  Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
</IfModule>

<IfModule mod_deflate.c>
  # Comprime archivos para que se descarguen más rápido
  AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css
  AddOutputFilterByType DEFLATE application/javascript application/x-javascript
  AddOutputFilterByType DEFLATE application/json application/xml
  AddOutputFilterByType DEFLATE image/svg+xml
</IfModule>

<IfModule mod_expires.c>
# Tiempo que el navegador guarda en caché
  ExpiresActive On
  
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/jpg "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/gif "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  ExpiresByType image/x-icon "access plus 1 year"
  
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
  
  ExpiresByType text/html "access plus 0 seconds"
  
  ExpiresByType font/woff "access plus 1 year"
  ExpiresByType font/woff2 "access plus 1 year"
  ExpiresByType application/font-woff "access plus 1 year"
  ExpiresByType application/font-woff2 "access plus 1 year"
  
  ExpiresByType video/mp4 "access plus 1 year"
  ExpiresByType video/webm "access plus 1 year"
</IfModule>

# Bloquea el listado de directorios
Options -Indexes

# Bloquea archivos ocultos
<FilesMatch "^\.">
  Order allow,deny
  Deny from all
</FilesMatch>

# Bloquea archivos de configuración
<FilesMatch "(package\.json|package-lock\.json|\.gitignore|\.gitattributes)$">
  Order allow,deny
  Deny from all
</FilesMatch>

# Fuerza HTTPS (descomentar cuando SSL esté activo)
# <IfModule mod_rewrite.c>
#   RewriteEngine On
#   RewriteCond %{HTTPS} off
#   RewriteRule ^(.*)$ https://%{HTTP_HOST}/$1 [R=301,L]
# </IfModule>

# Redirige www a no-www
# <IfModule mod_rewrite.c>
#   RewriteEngine On
#   RewriteCond %{HTTP_HOST} ^www\.(.*)$ [NC]
#   RewriteRule ^(.*)$ https://%1/$1 [R=301,L]
# </IfModule>

# Le indica a apache que tipo de archivos son
<IfModule mod_mime.c>
  AddType application/javascript js
  
  AddType image/webp webp
  AddType image/svg+xml svg
  
  AddType font/woff woff
  AddType font/woff2 woff2
</IfModule>

# Página de error 404
 ErrorDocument 404 /404.html

AddDefaultCharset UTF-8

# Mantiene la conexión abierta entre el cliente y el servidor
<IfModule mod_headers.c>
  Header set Connection keep-alive
</IfModule>

FileETag MTime Size
